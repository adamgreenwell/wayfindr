## Summary

- 

## Why

- 

## Validation

- [ ] `composer test` from `apps/server` when PHP/Laravel behavior changed
- [ ] `npm test --prefix packages/widget-js` when widget behavior changed
- [ ] `npm run build --prefix apps/server` when dashboard assets changed
- [ ] `make self-host-test` when installer, Compose, backup, restore, or release contracts changed
- [ ] `make public-info-check`
- [ ] `make wiki-test` when `docs/wiki` changed
- [ ] Not applicable; docs/config-only change

## Impact

- User-facing impact:
- Security/privacy impact:
- Migration or operations impact:

## Public information check

- [ ] This PR does not include secrets, credentials, real visitor data, private infrastructure details, or non-public business context.
- [ ] Security vulnerabilities, if any, were reported through the private vulnerability path instead of public issue text.
