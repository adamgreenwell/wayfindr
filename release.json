{
    "_comment": [
        "What the release being prepared requires of an operator (ADR 0012, ADR 0013).",
        "Authored by hand, like the CHANGELOG's Unreleased section; version and commit",
        "are NOT here because they are derived at build time from the release identity.",
        "`requires_operator_action` is derived too, from whether `actions` is empty -",
        "NOTICES ARE NOT COUNTED, because a notice-only release is safe to take",
        "unattended. See docs/self-hosting/release-manifest.md for the field reference."
    ],
    "_bootstrap": [
        "The bootstrap constraint is DISCHARGED: 0.1.0 shipped the guard declaring",
        "nothing, as ADR 0013 requires of the first enforcing release - one that",
        "refused traffic the moment it landed would punish installs already doing",
        "the right thing. Later releases MAY declare actions."
    ],
    "_backups_queue_consumer": [
        "RESOLVED. This entry used to record why the backups-queue-worker",
        "requirement was deliberately NOT declared, and the condition it named has",
        "now been met, so it is declared - as a NOTICE.",
        "The objection was never to the requirement, which is real, but to the",
        "response available. The only phase it could have taken was `after-start`,",
        "and an unmet after-start ACTION gates SERVING: an install missing a",
        "backups worker would have refused all traffic, taking the whole support",
        "platform down because a BACKUP worker was absent - on host-managed",
        "installs only, since compose stacks start that worker themselves, whose",
        "operator would then have to edit .env and restart with their site",
        "already down.",
        "ADR 0013 recorded the gap and named this requirement as the first",
        "candidate for a third, non-blocking response. That response now exists:",
        "`notices` is reported on the operator console, in wayfindr:upgrade-guard,",
        "and in the installer's upgrade output, and the gates cannot see it.",
        "So the response is now proportionate to the requirement, which is the",
        "test that was always being applied - not whether the check existed.",
        "It is verified by `check` rather than attested, so it retires itself the",
        "moment a worker is actually seen; an operator who already runs one is",
        "never told anything. It is also still reported where the operator meets",
        "it - the backups page shows whether a worker has been seen - because the",
        "person configuring backups should not have to read a release manifest."
    ],
    "_php_runtime_extensions": [
        "Version 0.8.0 is the first stable line whose host install declares curl,",
        "gd, and intl as Composer platform requirements. The official image bakes",
        "all three, so asking its operator to acknowledge host work would create a",
        "false outage. A host-managed deploy uses operator-provided PHP, and",
        "Composer stops before the application guard can explain a missing module.",
        "The action applies to host upgrades; a fresh host still owes the same",
        "ordinary baseline before its first deploy, but has no upgrade action.",
        "The installation_profiles scope keeps the one release verdict truthful",
        "without applying host-PHP work to an image. Older manifests",
        "omit that optional field and continue to apply everywhere."
    ],
    "minimum_upgrade_from": "0.1.0-alpha.1",
    "actions": [
        {
            "id": "php-runtime-extensions",
            "summary": "Prepare the host-managed PHP runtime before upgrading to this release.",
            "detail": "This applies when upgrading Laravel Forge and other deployments that run Wayfindr on host PHP. It does not apply to a known-fresh install, the published container image, or an image built locally with Wayfindr's Dockerfile. Before updating an existing checkout, enable curl, gd, and intl for PHP 8.4.1 or newer as used by Composer and the application; curl must use libcurl 7.59.0 or newer. On Debian or Ubuntu with PHP 8.4 the packages are normally php8.4-curl, php8.4-gd, and php8.4-intl. Verify Composer, PHP-FPM, queue, scheduler, and Reverb runtimes; reload PHP-FPM and restart long-running workers. Then add <release>/php-runtime-extensions to WAYFINDR_ACKNOWLEDGED_ACTIONS and deploy. Forge also asks for that key when absent state cannot prove a site is fresh. The guard rejects a failed CLI check even when that key is present; a passing CLI check remains unevaluable because it cannot prove the other runtimes.",
            "phase": "before-pull",
            "depends_on_release": "none",
            "installation_profiles": [
                "host"
            ],
            "applicability": {
                "type": "always"
            },
            "verification": {
                "type": "check",
                "check": "php-runtime-extensions"
            }
        }
    ],
    "notices": [
        {
            "_note": [
                "No `phase` and no `depends_on_release`: a notice never blocks, so",
                "there is nothing to time and nothing to strand. Running a queue",
                "worker needs no particular release's code, which is what makes",
                "this legitimately advisory rather than an action in disguise.",
                "`detail` DOES NOT print a copy-paste command, deliberately.",
                "`BACKUP_QUEUE` can move the backups queue off its default, and a",
                "manifest is static text - it cannot substitute this install's",
                "configured queue or timeout. A hard-coded `--queue=backups` would",
                "tell such an operator to start a worker draining a queue nothing",
                "dispatches to, leaving backups queued forever after they did",
                "exactly as they were told, with the check still reporting.",
                "OperatorBackupSettingsController::workerObservation() already",
                "carries that warning, and the backups page builds the command from",
                "the live config - so the notice names the requirement and points",
                "at the surface that can get the command right.",
                "The Compose worker follows BACKUP_QUEUE too (compose.yml interpolates",
                "it into --queue), so the claim that Compose stacks are already",
                "covered holds for a custom queue as well - it did not before."
            ],
            "id": "backups-queue-consumer",
            "summary": "Run a queue worker on the backups connection.",
            "detail": "Without one, \"Run a backup now\" queues a job nothing will process and the run sits at Running indefinitely. Scheduled backups are unaffected. Compose stacks run this worker themselves, on whichever queue BACKUP_QUEUE names. On a host-managed install, open /operator/settings/backups - it prints the exact command for this install, with your configured queue name and timeout filled in (BACKUP_QUEUE can move the queue off its default, so a copied command may drain the wrong one).",
            "applicability": {
                "type": "always"
            },
            "verification": {
                "type": "check",
                "check": "backups-queue-consumer"
            }
        }
    ]
}
